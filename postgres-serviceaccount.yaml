# Service account per il pod Postgres.
# Riceve via Pod Identity association il role BreakingProdSecretsReaderRole
# che ha permesso secretsmanager:GetSecretValue su breaking-prod/postgres-password
apiVersion: v1
kind: ServiceAccount
metadata:
  name: postgres-sa
